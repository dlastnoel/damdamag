import 'package:flutter/material.dart';

class CovidCases {
  Color color;
  String imageUrl;
  String count;
  String category;

  CovidCases(
      {@required this.color,
      @required this.imageUrl,
      @required this.count,
      @required this.category});
}
